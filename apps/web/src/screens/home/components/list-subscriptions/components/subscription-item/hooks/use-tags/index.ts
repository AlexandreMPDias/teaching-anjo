import { IBillSubscription } from '@angel-oak/core/models/schemas';
import { TagData } from './types';

export const getTags = (subscription: IBillSubscription): readonly TagData[] => {
	const tags: TagData[] = [];

	return [];
};

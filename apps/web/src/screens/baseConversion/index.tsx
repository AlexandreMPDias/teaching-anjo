import { Button, useDisclosure, Center } from '@chakra-ui/react';
import { useEffect, useRef } from 'react';
import { Screen } from '@angel-oak/ui/layouts/screen';

export const BaseConversionScreen: React.FC = () => {
	return <Screen display="flex" flexDirection={'column'} pb="20px"></Screen>;
};
